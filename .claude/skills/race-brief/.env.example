# NWS (api.weather.gov) asks for an identifying User-Agent with contact info.
# Copy to .env and set your own (name/app plus a contact address in parentheses).
# This is NOT a secret or API key — it just identifies the caller to NWS.
NWS_USER_AGENT="bristol-race-brief (contact: YOUR_CONTACT_HERE)"
