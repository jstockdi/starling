---
description: Quick wind glance for Bristol Harbor / Hog Island / East Passage
argument-hint: [date] [start-end] (defaults to today, 1100-1600)
---

Give me a quick **wind-only** picture for racing out of Bristol Harbor using the
`race-brief` skill.

Race window: **$ARGUMENTS** (default to today, 1100–1600 if not given).

Run `fetch.py` + `plot.py`, then summarise: live Conimicut obs, race-window average
and gust, the fix gradient (inner harbor vs. Hog Island vs. East Passage), and
expected shifts / sea-breeze timing. Show the wind-gradient chart. Keep it tight —
no tide or current unless I ask.
